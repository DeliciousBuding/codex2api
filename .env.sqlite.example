# ============================================================
# Codex2API SQLite 轻量版环境变量配置
# 复制此文件为 .env 并按需修改
# ============================================================

# HTTP 服务端口
# 注意：docker-compose.sqlite.yml 默认将端口绑定到 127.0.0.1（仅本机访问）
# 如需从其他机器访问，请修改 compose 文件中的 ports 配置，删除 "127.0.0.1:" 前缀
CODEX_PORT=8080

# 管理后台登录密钥（可选，设置后首次访问 /admin 会弹出密码输入框）
# ADMIN_SECRET=your-admin-password

# ---- 数据库 / 缓存驱动 ----
DATABASE_DRIVER=sqlite
CACHE_DRIVER=memory

# ---- SQLite ----
DATABASE_PATH=/data/codex2api.db

# 下游 API 密钥鉴权（逗号分隔，留空则不鉴权）
# CODEX_API_KEYS=sk-your-key-1,sk-your-key-2

# 全局代理（可选）
# CODEX_PROXY_URL=http://host.docker.internal:7890

# 快速调度器（可选，也可在管理后台运行时开启）
# FAST_SCHEDULER_ENABLED=true

# ---- 时区 ----
TZ=Asia/Shanghai
